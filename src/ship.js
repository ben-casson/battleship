export const createShip = (length) => {
    //array of Maps, each Map contains an 'x' key and a 'y' key
    const coordinates = [];
    function setCoordinates() {

    }
    return { length, coordinates, setCoordinates };
}